# Investigate

Read-only. No edits, no commits, no schema talk presented as a change.

1. Read this repo's `AGENTS.md` or `CLAUDE.md` before searching the tree.
2. Call `how` for mechanics (the path that runs, the write, the read).
3. Answer with Overview, How it works, Where things live, Gotchas. Cite files and symbols. If a search is empty, say so.
4. If the question is "are we sure" or "should we do X or Y", give a recommendation with the tradeoff. Push back if the premise is wrong.
5. Do not fix anything extra.
6. If the user then wants a change, re-match (`bug-fix`, `feature`, or `refactoring`).
